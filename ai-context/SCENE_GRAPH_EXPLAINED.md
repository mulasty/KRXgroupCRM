# Scene Graph Schema

This schema describes the structure of the WebGL scene.

Main components:

Camera
Controls the view and responds to scroll and cursor.

Lighting
Includes ambient, key, rim and cursor lights.

Layers
Environment layer
Particle layer
Mockup layer
Interaction layer
UI layer

Each layer is responsible for specific scene responsibilities.